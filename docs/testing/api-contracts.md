# API Contract & Schema Drift Testing

- Use `src/lib/contracts/schemaContract.ts` helpers to compare schema snapshots.
- Keep schema snapshots in `docs/testing/schema-snapshots/`.
- Run contract tests in CI before merge.
